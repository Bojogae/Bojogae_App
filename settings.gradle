

pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        gradlePluginPortal()
        flatDir {
            dirs 'aars'
        }
    }
}



dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
        flatDir {
            dirs 'aars'
        }
    }

}


rootProject.name = "Face-Detect-Distance-OpenCV-UVC"
include ':app'
include ':opencv'
include ':libusbcamera'
